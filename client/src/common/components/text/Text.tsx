import styled from 'styled-components';
import { CenteredRow } from '../layout';

export const Text = styled(CenteredRow)`
    color: var(--text-color);
    font-size: var(--font-size);
    font-family: var(--font-family);
    text-align: center;
`;